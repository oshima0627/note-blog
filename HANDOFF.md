# HANDOFF

最終更新: 2026-08-28（vs比較の3本目を公開）

## いま何をしているのか

目的: note販売で月10万円。方針は本人決定で「記事販売一本」。

やることは2つ。
1. **流入を6倍にする**（実証済みの「vs比較 × 初心者」型を増やす）← **3本目まで公開済み**
2. その流入を商品に到達させる（CTA導線・16本実装済み）＋ **商品を読者に合わせる** ← 次はここ

## 実態（note API で実測。数字は 2026-08-27 時点の監査）

詳細: `analytics/2026-08-27_note-account-audit.md` / `_sales-reality.md` / `_audience-product-mismatch.md`

- 公開114本（監査時点）/ 全期間 40,875ビュー / フォロワー259 / 直近30日 1,677ビュー
- **生涯売上 ¥13,880**（14ヶ月・手数料前）。**2026年7月 ¥0 / 8月 ¥0**
- 未振込 ¥6,940（振込可能 ¥5,460）。**本人判断で据え置き。たまってから申請する**
- 売上ゼロの構造的原因: 流入上位11本＝27,856ビュー（全体の68%）はすべて初心者向け。
  一方 ¥3,980帯4本はすべて事業者向けで合計126ビュー。**来ている人と売っている物が別人向け**

## 今回やったこと（2026-08-28）

### vs比較の3本目を公開した

**https://note.com/oshima0627/n/ncf5da0d7b604**（key `ncf5da0d7b604` / 2026-08-28 16:05 JST 公開）

- 「Claude Code vs GitHub Copilot｜初心者はどっちから始めるべき？【2026年8月版】」
- 無料 / 大見出し9 / 表0 / 埋め込みCTA2枚（入口A ¥500 `nd69e07a7c204` + 本命 ¥1,980 `nf6dc3eb8e78d`）/ タグ5
- 切り口は1・2本目と変えた。**3本目=「補完が出発点」か「エージェントが出発点」か＋0円で始められるか**
- 原稿は前セッションで執筆済み（公式8ページで裏取り）。今回は公開作業のみ

### 変更したファイル

- `articles/drafts/2026-08-27_claude-code-vs-github-copilot-beginner.md`
  - 本文中の `` `@copilot` `` からバックティックを削除（**note は記号を生で出す**ため）
  - 先頭に「公開済み: URL」のコメントを追加
- `articles/published.json` に id:66 を追記（**29行の追記のみ・削除0**）
- `analytics/cta-funnel-plan.md` に「noteエディタの操作（2026-08-28）」を追記

### 手順が1つ楽になった（次回はこれを使う）

**大見出しは Ctrl + Alt + 2 のキーボードショートカットで付けられる。**
これまでの「ツールバーの座標を毎回測り直す」は不要。詳細は `analytics/cta-funnel-plan.md` の末尾。

## 検証済みの事実（実際に画面に出した出力のみ）

- **公開後に note API `/api/v3/notes/ncf5da0d7b604` で確認:**
  `status:"published"` / `price:0` / `publish_at:2026-08-28T16:05:21+09:00` /
  `<h2>` **9個** / `<h3>` 0 / `<table>` **0個** / `<figure>` **2個** /
  本文に `nd69e07a7c204` と `nf6dc3eb8e78d` の両方が存在 / `**` 0 / `|` 0
- **ハッシュタグ5件を `data.hashtag_notes` で確認**:
  #ClaudeCode / #GitHubCopilot / #プログラミング初心者 / #AI開発 / #副業
- **本文の転記ミスがゼロであることを照合で確認**: エディタの全ブロックを join した文字列と
  リポジトリ原稿から作った文字列が **len 4038 / hash 1019815352 で完全一致**（見出し設定後も同一）
- **埋め込みカード2枚がスクリーンショットで正しい記事を指していることを目視確認**
  （¥500「知識ゼロでもできる｜Claude Codeで業務を半分にする5つの使い方」/
  ¥1,980「Claude Code完全攻略｜AI時代の副業革命で月10万円を確実に稼ぐ最強ロードマップ」）
- `articles/published.json` は JSON としてパース可能・66本・`git diff` は **29行の追記のみ・削除0**

## 未検証のもの

- **実機で Copilot CLI / Copilot Free / Claude Code を動かしてはいない。**
  記事の記述はすべて公式ページの記載（前セッションで8ページを取得済み）
- Copilot Free の「AIクレジットの枠」は公式に数値の記載が無かった。**本文でも数値を書いていない**
- **vs比較3本（`n54370e748c01` / `nfd25ea1dabf6` / `ncf5da0d7b604`）とCTA16本の効果はまだ測っていない。**
  最短でも公開2週間後（`n54370e748c01` 基準で 2026-09-10 以降）に測る

## 次にやること

1. **¥1,980と¥3,980の間に、初心者向けの本命商品を作る**（いまここが空白。**最優先**）。
   候補「プログラミング未経験から Claude Code で最初のWebアプリを公開するまで」。
   ※ **実機で手順を通してから書くこと**
2. 主力 ¥1,980 記事 `nf6dc3eb8e78d` にクロスセルを入れる（現在リンク0本）
3. 4本目を出すなら候補は「Opus 5 と Sonnet 5 の使い分け」/「Claude Code vs Gemini CLI（2026年版）」。
   手順は `analytics/cta-funnel-plan.md` の「新規記事の公開手順」＋末尾の「noteエディタの操作」
4. 有料26本の棚卸し（FX・競馬・AI BGM など Claude と無関係な商品が7本以上）
5. 2週間後に `/api/v1/stats/pv?filter=monthly` でビュー変化を測る

## 触ってはいけないところ

- **記事で扱う製品は、料金ページだけでなく製品の概要ページを必ず開いてから書く。**
  1本目（vs Cursor `n54370e748c01`）で「ターミナル専用」「Node.js が必要」の2件を公開後に訂正した
- **note の本文に Markdown 記法を持ち込まない。** `**` も表もバックティックも生テキストで出る
- **noteエディタ: Enter は段落を分割しない（brになる）。** 段落は `text/html` の paste で入れる。
  URLの埋め込みカードは「段落を空にしてから `text/plain` の URL を paste」。**複数ある場合は後ろから**
- **JS でカーソルを置いてから `type` しない**（英数字が脱落する）。
  ただし**キーボードショートカット（Ctrl+Alt+2 等）は JS 選択と併用して問題ない**（3本目で実証）
- **見出しは取りこぼす。** 適用後に必ず H2 の index 一覧を照合して抜けを再実行する
- **数値IDのエディタURLを開いて保存しない。** key形式を使う
- **`.ProseMirror` が出るまで（約10秒）保存しない**
- **パスワード・本人確認情報は Claude が入力しない。** 売上確認・振込は本人が行う
- **振込申請を「次にやること」として再提案しない。** 2026-08-27 に本人が
  「たまってから申請する」と決めた。放置ではなく判断済みの保留
- **公開（note への投稿）は毎回、本人の許可を取ってから実行する**
- **政治記事（立憲民主党・自民党・参政党）と株価予想記事に商品CTAを貼らない**
- **「Claude Code企業導入成功事例：売上10億円増加」（540ビュー）は数値未確認。** 導線に組み込まない
- 記事の自動生成ルーティン2本は停止したまま（`trig_01QzBCjYYyTexPkS5XJ1FhNm` /
  `trig_01DD4hdtnLL3RBxuWGHqq3nH`）。**ニュース量産は流入も売上も生まないので再開しない**
- `articles/published.json` は**末尾に追記するだけ**にする。
  JSON を再書き出しすると `8.0` → `8` のような無関係な差分が出るので、出たら戻す
- 出典が取れない数値・事例は書かない。第三者メディアの数値を公式扱いしない
- リモートに作業ブランチを作らず、`git push origin HEAD:main` で main に直接反映する
- 空の下書き `n012952dc5512` が1件残っている（操作ミス由来・実害なし）
