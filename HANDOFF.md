# HANDOFF

最終更新: 2026-08-27（note収益の実測と導線改修に着手）

## いま何をしているのか

**目的: note販売で月10万円。** そのために「記事を増やす」のではなく
**既存の流入を有料記事に到達させる**方向へ舵を切った。

2026-08-27 に note アカウント（note.com/oshima0627）を実測したところ、
記事本数ではなく**導線**がボトルネックだと分かった。データは
`analytics/2026-08-27_note-account-audit.md` に保存。

## 実測データ（note API から取得・確認済み）

- 公開113本 / 全期間 40,875ビュー / フォロワー259 / 直近30日 1,677ビュー
- **有料26本の全期間ビューは合計1,519（全体の3.7%）、中央値29ビュー**
- **有料記事へのリンクを1本も持たない無料記事が73本、18,197ビュー（全体の45%）**
- 上位3本で20,053ビュー（49%）。上位はすべて2025年7月の「vs比較」記事
- 2026年に量産したニュース記事は1本平均27〜41ビュー（2025年7月は平均870）
- マガジン0本・メンバーシップ未設定（月額収入源なし）

## 今回やったこと

1. note API（`/api/v1/stats/pv`, `/api/v3/notes/{key}`, `/api/v2/creators/...`）で
   全113本のビュー・価格・内部リンク数を取得し `analytics/` に保存
2. 商品ラダーを定義し、CTA の型と貼付手順を `analytics/cta-funnel-plan.md` に文書化
3. **導線ゼロの高流入記事3本に CTA を追加し、公開まで完了**（下記「検証済み」）

## 検証済みの事実（実際に画面／APIで確認した出力のみ）

- CTA追加後、`/api/v3/notes/{key}` で `ctaLinks: 2` / `status: "published"` を確認:
  - `na50387f601d3` Claude活用術（1,260ビュー・直近30日214）
  - `n8eb210341bbc` Claude Code vs Codeium（1,523ビュー）
  - `n0a702dddb34e` 生成AIの仕組み完全解説（1,496ビュー・直近30日126）
- 「更新する」で再公開しても**公開日は変わらない**（`publishAt` 未変化を確認）
- note 利用規約の禁止事項に、自アカウントの自動編集を直接禁じる条項は**無い**。
  効く制約は「当社のサービスの障害となる行為」。→ 連打せず低速で行う
- エディタURLは **key 形式**（`/notes/n8eb.../edit/`）。数値ID形式は本文0文字で開く

## 未検証のもの

- **売上金額・購入件数は取得できていない。** `/api/v1/stats/sales` が
  `user_verification_needed` を返す。note の売上ページは**パスワード再確認**が必要で、
  Claude はパスワードを入力しない。**本人が実施すること**（下記「次にやること」）
- CTA追加による転換率の変化は**まだ測っていない**。効果測定は最低2週間後

## 次にやること

1. **本人作業**: https://note.com/dashboard/salesmanage でパスワードを入れ、
   実売上（金額・件数・どの記事が売れたか）を確認して共有する。
   これが分かるまで価格設計は推測になる
2. 残りの高流入・導線ゼロ記事に CTA を追加する。
   優先順とURL・入口の割り当ては `analytics/cta-funnel-plan.md` の「次の優先順」に記載。
   手順とハマりどころも同ファイルにある（**JSでカーソルを置くと壊れる。実クリック必須**）
3. 主力 ¥1,980 記事 `nf6dc3eb8e78d` 自身にクロスセルリンクを入れる（現在0本）
4. 検索流入型の新規記事（「vs比較」「完全ガイド」型）を書く。
   毎日のニュース記事は平均27ビューで流入を生んでいないため再開しない
5. 2週間後に `/api/v1/stats/pv?filter=monthly` で有料記事のビュー変化を測る

## 触ってはいけないところ

- **note エディタで JS からカーソルを設定して type しない。** Enterが無視され英数字が脱落する。
  必ず実際のマウスクリックでフォーカスを取る。壊したら Ctrl+Z ×40（公開版は更新前なら無傷）
- **数値IDのエディタURLを開いて保存しない。** 本文0文字で開くため記事を消す恐れがある
- **パスワード・本人確認情報は Claude が入力しない。** 売上確認は本人が行う
- **政治記事（立憲民主党・自民党・参政党）に商品CTAを貼らない。** 読者層が違う
- 記事を更新する前に必ず本文が読み込まれたことを確認する（7〜8秒待つ）
- 記事の自動生成ルーティン2本は 2026-08-27 に停止したまま（`trig_01QzBCjYYyTexPkS5XJ1FhNm` /
  `trig_01DD4hdtnLL3RBxuWGHqq3nH`）。ニュース量産は流入を生まないので再開しない方針
- `articles/published.json` は末尾に追記するだけにする（全体書き直しは差分が膨れる）
