# HANDOFF

最終更新: 2026-08-29（有料記事の「無料エリア」に初めて導線を入れた）

## いま何をしているのか

目的: note販売で月10万円。方針は本人決定で「記事販売一本」。

やることは2つ。
1. 流入を増やす（「vs比較 × 初心者」型）← 無料3本公開済み
2. 商品を読者に合わせ、流入を商品に到達させる ← **今回ここを1段進めた**

次は **4本目の無料記事（流入）** と **2026-09-11 以降の効果測定**。

## 今回やったこと（2026-08-29）

### 発見: 有料記事のCTAは購入者にしか見えていなかった

`https://note.com/api/v3/notes/<key>` は未ログインだと無料プレビュー分の body しか返さない
（`can_read:false`）。これで調べたところ、有料3本とも**無料プレビュー内の自記事リンクが0本**だった。
公開ページのHTMLでも同じ。つまり 8/28 に ¥3,980 に入れたCTA2本も有料エリア内で、
購入者0人の現在は誰にも見えていなかった。

→ 引き継ぎの「¥1,980 の末尾にクロスセル」は、見えるのが購入者2人だけなので価値が低いと判断。
本人に確認して **「有料ラインより前（無料プレビュー内）に入口Aへのリンクを置く」** に変更した。

### 実施（2本。どちらも `PUT /api/v1/text_notes/<id>` が 200）

- **`nf6dc3eb8e78d`（¥1,980・547ビュー）**
  「あなたはどちらのマインドセットを選ぶ？」の直後、区切り線＋第3章H2の前に挿入
  「ここから先は有料エリアです。まず500円で試したい方は、入門編としてこちらもあります。」
  ＋ 入口A `nd69e07a7c204` の埋め込みカード
- **`n621957de7745`（¥3,980・8/28公開の本命）**
  「macOS と Linux では通していません…」の直後、
  「ここから先は、実際に手を動かす部分です。」の前に挿入
  「まず500円で小さく試したい方には、入門編としてこちらもあります。」
  ＋ 入口A `nd69e07a7c204` の埋め込みカード

`nd69e07a7c204`（入口A自身）は**意図的に除外**。無料プレビューが1段落しかなく、
そこに置けるのは上位商品へのリンクだけになる。購入判断の直前に高い商品を出すのは逆効果と判断。

### 「更新する」でページが固まる問題の原因を特定した

`nf6dc3eb8e78d` で2回連続で固まった（スクショもread_pageも injection timeout）。
ページ内に fetch/alert のフックを入れてから押して、実測でつかんだ:

**`PUT /api/v1/text_notes/119031232` が 422 → note が
`window.alert("数量限定販売をするにはプレミアムユーザになる必要があります")` を出し、
ネイティブalertがレンダラを止めていた。**

この記事は `prior_sale: {activated:true, first_served_sales_count:10, stock_count:8}`。
公開設定の「セール」で **「設定しない」を選んでから更新すると 200 で通った。**

手順・診断用スニペットは `analytics/cta-funnel-plan.md` の末尾に全部書いた。

## 検証済みの事実（実際に画面に出した出力のみ）

### `nf6dc3eb8e78d`（¥1,980）更新後

- `status:"published"` / `price:1980` / `publish_at` は **2025-07-14T08:00:00 のまま**
- `separator` は `9b721242-864c-465d-81f0-f357df82e8c5`（第3章H2）で**変わっていない**
- 未ログインの無料プレビュー body に `nd69e07a7c204` の埋め込みが出現（`<figure>` 2→3）
- 公開ページHTML（未ログインcurl）に CTA本文と埋め込みリンクの両方を確認
- **`prior_sale.activated` は更新後も `true`、公開ページも「数量限定：残り 8 / 10」を表示したまま。**
  「設定しない」を選んで保存したのに表示が残る理由は**未検証**

### `n621957de7745`（¥3,980）更新後

- `status:"published"` / `price:3980` / `publish_at` は **2026-08-28T17:03:46 のまま**
- `separator` は `d3c5b128-1de4-4a80-85ec-15ffa0453ce7`（「ここから先は、実際に手を動かす部分です。」）
  で**変わっていない**
- 無料プレビュー body（2,905字）に `nd69e07a7c204` の埋め込み1件
- 公開ページHTMLでもリンク・CTA本文を確認

### note エディタの挙動（今回あらためて実測）

- `computer` の `key: Return` は段落を分割せず `<br>` になる（再確認）
- `ClipboardEvent` の `text/html` は、**先頭に空 `<p>` を入れないと**1つ目の段落が
  カーソル位置の既存段落に連結される（実際に1度連結された。Ctrl+Z で戻して直した）
- URLの埋め込みカードは `text/plain` の paste でしか作られない（`text/html` だとただの文字列）
- `scrollIntoView` は効かない（再確認）
- エディタ読み込み直後の1クリック目は効かないことが多い

## 未検証のもの

- **売れるかどうかは未測定。** 今回の変更の効果が出るのは最短で 2026-09-11 以降
- **`prior_sale` が「設定しない」保存後も activated:true のまま残る仕組みは不明。**
  次に ¥1,980 を編集するときは、更新前後で `prior_sale` と公開ページ表示を必ず確認する
- 購入者から見た表示（有料エリアの見え方）は未確認。見ているのは著者側のAPIと公開ページのみ
- 入口A `nd69e07a7c204` / 入口B `n126bf1838ba3` のビュー数を持っていない
- 旧 GitHub Pages URL `https://oshima0627.github.io/jikyu-log/` の状態は 8/28 以降未確認

## 見つけたが直していない問題（¥1,980 記事の中身）

無料プレビューを読んで気づいた。**今回は触っていない。**

- 「開発サイクル30%短縮 / コード品質：大幅向上 / 学習コスト90%削減 / プロジェクト成功率85%向上」
  を「複数の企業での導入事例によると」として出典なしで書いている
  → CLAUDE.md のハルシネーション対策に反する
- Markdownの表がそのまま生テキストで出ている
  （「| 従来のプログラミング | Claude Code ||---|---|」）

¥1,980 の主力商品なので、直すかどうかは本人判断。

## 次にやること

1. **4本目の無料記事**（流入レバー）。候補は「Opus 5 と Sonnet 5 の使い分け」/
   「Claude Code vs Gemini CLI（2026年版）」。**書く前に公式の概要ページと料金ページを両方開く**
2. **2026-09-11 以降**に `/api/v1/stats/pv?filter=monthly` でビュー変化と売上を測る。
   特に ¥3,980 の初心者向け `n621957de7745` が売れるかが監査の仮説の答え合わせ
3. 上の「¥1,980 の出典なし数値と生の表」を直すか決める
4. 有料27本の棚卸し（FX・競馬・AI BGM など Claude と無関係な商品が7本以上）

## 触ってはいけないところ

- **note編集を自動操作するときは、押す前に `window.alert` を差し替えるフックを入れる。**
  入れないと 422 のたびにページが固まって原因が分からなくなる
  （スニペットは `analytics/cta-funnel-plan.md`）
- **`nf6dc3eb8e78d` は数量限定販売が有効。** 本文を直すには公開設定の「セール」を
  「設定しない」にしてから更新する。そのまま更新すると 422 で保存できない
- **`jikyu-log` のリポジトリと Worker を消さない。**
  有料記事 `n621957de7745` の本文が https://jikyu-log.oshima6-27.workers.dev を実物として参照している
- **記事で扱う製品は、料金ページだけでなく製品の概要ページを必ず開いてから書く**
- **手順記事は実機で通してから書く。** 通していない部分は「未検証」と本文に明記する
- **note の本文に Markdown 記法を持ち込まない。** `**` も表もバックティックも生テキストで出る
- **クリック座標は毎回 DOM から計算し、打つ前に `document.activeElement` を確認する**
- **`.ProseMirror` が出るまで（約10〜15秒）保存しない**
- **数値IDのエディタURLを開いて保存しない。** key形式を使う
- **パスワード・本人確認情報は Claude が入力しない。** 売上確認・振込は本人が行う
- **振込申請を「次にやること」として再提案しない。** 2026-08-27 に本人が判断済みの保留
- **公開・公開済み記事の変更は毎回、本人の許可を取ってから実行する**
- **政治記事（立憲民主党・自民党・参政党）と株価予想記事に商品CTAを貼らない**
- **「Claude Code企業導入成功事例：売上10億円増加」（540ビュー）は数値未確認。** 導線に組み込まない
- 記事の自動生成ルーティン2本は停止したまま。ニュース量産は流入も売上も生まないので再開しない
- `articles/published.json` は**末尾に追記するだけ**。JSON を再書き出しすると無関係な差分が出る
- リモートに作業ブランチを作らず、`git push origin HEAD:main` で main に直接反映する
- 空の下書き `n012952dc5512` が1件残っている（操作ミス由来・実害なし）
