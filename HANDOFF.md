# HANDOFF

最終更新: 2026-08-27（note収益の実測 → 導線改修11本を実施）

## いま何をしているのか

**目的: note販売で月10万円。** 方針を「記事を増やす」から
**既存の流入を有料記事に到達させる**に切り替えた。

2026-08-27 に note（note.com/oshima0627）を API で実測したところ、
ボトルネックは記事本数ではなく**導線**だと判明した。
実測データ: `analytics/2026-08-27_note-account-audit.md`
導線の設計と手順: `analytics/cta-funnel-plan.md`

## 判明した実態（note API で取得）

- 公開113本 / 全期間 40,875ビュー / フォロワー259 / 直近30日 1,677ビュー
- **有料26本の全期間ビューは合計1,519（全体の3.7%）、中央値29ビュー**
- 上位3本で20,053ビュー（49%）。上位はすべて2025年7月の「vs比較」記事
- 2026年に量産したニュース記事は1本平均27〜41ビュー（2025年7月の記事は平均870）
- マガジン0本・メンバーシップ未設定（月額収入源が無い）

**月10万円の距離**: 手取り10万円 ≒ 売上11.7万円。1,980円なら月59本、3,980円なら月30本。
現状の月1,677ビュー（ほぼ無料記事）では届かない。必要なのは商品ページ到達数の10〜20倍化。

## 今回やったこと

1. 全113本のビュー・価格・内部リンク数を API で取得し `analytics/` に保存
2. 商品ラダー（500円入口2種 → 1,980円本命）を定義し、CTAの型と手順を文書化
3. **導線ゼロの高流入記事11本にCTAを追加し、公開まで完了**

## 検証済みの事実（実際にAPI/画面で出した出力のみ）

- CTA追加11本すべてで `/api/v3/notes/{key}` が
  `ctaLinks: 2` / `status: "published"` / `price: 0` を返すことを確認（一覧は cta-funnel-plan.md）
- **アカウント全体の再計測（実施後）**:
  - 導線ゼロの無料記事 **73本 → 62本**
  - 導線ゼロに死蔵されていたビュー **18,197 → 8,846**（9,351ビュー分を接続）
  - 導線のある無料記事のビュー合計 **20,053 → 30,510**（無料記事の51% → 78%）
- 「更新する」で再公開しても**公開日は変わらない**（`publishAt` 未変化を確認）
- note 利用規約の禁止事項に、自アカウントの自動編集を禁じる条項は**無い**。
  効くのは「当社のサービスの障害となる行為」→ 連打せず低速で行う
- エディタURLは **key 形式**（`/notes/n8eb.../edit/`）。数値ID形式は本文0文字で開く

## 未検証のもの

- **売上金額・購入件数は未取得。** `/api/v1/stats/sales` が `user_verification_needed` を返し、
  note の売上ページは**パスワード再確認**を要求する。Claude はパスワードを入力しない
- **CTA追加の効果はまだ測っていない。** 効果測定は最低2週間後
- `n012952dc5512`（id 177038167）に**空の下書きが1件**残っている。
  操作ミスで作られたもので、タイトル・本文とも空。記事一覧には出てこないので実害は無いが、
  気になるなら note の下書き一覧から手動削除する

## 次にやること

1. **本人作業（最優先）**: https://note.com/dashboard/salesmanage でパスワードを入れ、
   実売上（金額・件数・どの記事が売れたか）を確認して共有する。
   これが分かるまで価格設計は推測のままになる
2. 残りの導線ゼロ記事にCTAを追加（対象・入口の割り当ては cta-funnel-plan.md の
   「残っている導線ゼロ記事」）。**手順とハマりどころも同ファイルにある**
3. 主力 ¥1,980 記事 `nf6dc3eb8e78d` 自身にクロスセルリンクを入れる（現在0本）
4. 有料26本の棚卸し。FX・AI BGM・株価予想など Claude と無関係の商品がブランドを薄めている
5. 検索流入型の新規記事（「vs比較」「完全ガイド」型）を書く。
   この型だけが実際に流入を生んでいる。ニュース量産は平均27ビューなので再開しない
6. 2週間後に `/api/v1/stats/pv?filter=monthly` で有料記事のビュー変化を測る

## 触ってはいけないところ

- **note エディタで JS からカーソルを設定して type しない。** Enterが無視され英数字が脱落する
  （「▼ まず1本試すなら（500円）」→「▼まず本試すなら（円）」になった）。
  **実クリック → `ctrl+End` → カーソルが最終ブロックにあることをJSで検証 → type** の順で行う
- **`isLast` の検証を飛ばさない。** 検証を省いた回にCTAが記事の**冒頭**に挿入された。
  公開前に気づいて Ctrl+Z で復旧し、本文がAPIの公開版とバイト一致することを確認済み
- **数値IDのエディタURLを開いて保存しない。** 本文0文字で開くため記事を壊す恐れがある
- **パスワード・本人確認情報は Claude が入力しない。** 売上確認は本人が行う
- **政治記事（立憲民主党・自民党・参政党）と株価予想記事に商品CTAを貼らない。** 読者層が違う
- **「Claude Code企業導入成功事例：売上10億円増加」（540ビュー）は数値の裏取りが未確認。**
  CTAを貼って導線に組み込む前に事実確認すること
- 記事更新前に必ず本文が読み込まれたことを確認する（7〜10秒待つ）
- 記事の自動生成ルーティン2本は停止したまま（`trig_01QzBCjYYyTexPkS5XJ1FhNm` /
  `trig_01DD4hdtnLL3RBxuWGHqq3nH`）。ニュース量産は流入を生まないので再開しない方針
- `articles/published.json` は末尾に追記するだけにする
